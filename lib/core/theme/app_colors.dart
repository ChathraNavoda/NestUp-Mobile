import 'package:flutter/material.dart';

class AppColors {
  // Branding primary (logo/app icon)
  static const Color brandPrimary = Color(0xFF9A3412); // terra cotta

  // Main theme primary color
  static const Color primary = Color(0xFF0098B2); // teal/cyan

  // Secondary: complementary for buttons, highlights
  static const Color secondary = Color(0xFF00B0D0);

  // Accent: warm highlights (chips, tags)
  static const Color accent = Color(0xFFFACC15); // soft gold

  // Neutral: backgrounds, subtle containers
  static const Color neutral = Color(0xFFE5E5E5);

  // Dark: text/icons
  static const Color dark = Color(0xFF1C1917);

  // Light: text/icons on dark or primary backgrounds
  static const Color light = Color(0xFFFFFFFF);
}
